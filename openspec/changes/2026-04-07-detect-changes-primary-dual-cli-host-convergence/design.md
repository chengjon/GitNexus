## Design

This slice updates only governance and audit documents.

- reuse the current Codex and Claude Code host evidence as the truth source
- record the current machine constraint that no directly callable Cursor CLI is
  available for a no-new-environment live probe
- rewrite review and roadmap wording so the required host surface is the
  repository’s primary `Codex + Claude Code` pair
- keep Cursor and other clients as optional external follow-up, not as blocking
  debt
