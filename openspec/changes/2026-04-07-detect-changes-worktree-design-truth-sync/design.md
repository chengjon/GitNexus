## Design

This slice updates only design, review, and governance documents.

- use the current handler, git helper, and focused tests as the source of truth
- sync the historical design doc to the implemented contract
- leave the remaining open item explicitly scoped to external host
  compatibility research

This keeps the detect_changes worktree docs useful without reopening the
runtime implementation.
