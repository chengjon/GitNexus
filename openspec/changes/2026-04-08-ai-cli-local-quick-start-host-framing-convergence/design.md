## Design

This slice updates only documentation.

- treat the shared README, secondary entrypoint, and dual-CLI host-governance
  conclusions as the truth source for host-scope wording
- update the AI CLI local quick start to state that the local fork's primary
  maintained CLI surface is `Claude Code + Codex`
- keep the document's concrete MCP expectations scoped to those two maintained
  CLI hosts and avoid implying equal maintenance scope for every external host
