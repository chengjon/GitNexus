## Design

This slice updates only documentation.

- treat the already-closed dual-cli freshness convergence and current
  quick-start wording as the truth source
- update both the source `gitnexus-cli` skill and the package skill copy so
  stale-index troubleshooting uses host-neutral reconnect wording
- explicitly preserve Claude Code and Codex as the two named recovery paths
  without changing runtime behavior
