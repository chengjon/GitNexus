## Design

This slice updates only documentation.

- treat the current `detect_changes` metadata/path-resolution contract as the
  truth source
- update both the source `gitnexus-impact-analysis` skill and the package skill
  copy so they mention the current verification metadata
- keep the scope bounded to skill docs and governance records only
