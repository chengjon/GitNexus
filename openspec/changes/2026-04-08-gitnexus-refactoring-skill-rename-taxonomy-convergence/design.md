## Design

This slice updates only documentation.

- treat the current rename taxonomy as the truth source
- update both the source `gitnexus-refactoring` skill and the package skill
  copy so they use `graph` / `text_search`
- keep the scope bounded to docs and governance records only
