## Design

This slice updates only documentation.

- treat the current README prompt wording, the dual-CLI host-governance
  conclusion, and the earlier prompt-host convergence slice as the truth source
- update the root README so the `@gitnexus ...` invocation is explicitly marked
  as a Claude Code specific host example
- preserve Codex as part of the repository's primary maintained CLI surface
  without claiming equivalent prompt syntax where it is not documented here
