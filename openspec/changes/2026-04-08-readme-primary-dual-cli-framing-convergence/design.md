## Design

This slice updates only shared README and governance documents.

- treat the existing dual-CLI host-governance audit and roadmap conclusion as
  the truth source for the repository's primary maintained pair
- update the root and package READMEs so `Claude Code + Codex` appear first and
  are explicitly labeled as the primary surface
- keep other MCP hosts documented as optional integrations rather than deleting
  their setup instructions
