## Design

This slice updates only documentation.

- treat the shared README framing convergence and dual-CLI host-governance
  conclusion as the truth source for primary-vs-optional host wording
- update the quick-start guide to state the priority boundary explicitly while
  keeping optional host setup examples
- update the eval README so hook-style augmentation is described without
  implying equal maintenance scope for every external host
