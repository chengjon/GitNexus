## Design

This slice updates only documentation.

- treat the current README prompt wording and the dual-CLI host-governance
  conclusion as the truth source
- update the skills modification suggestions doc so the `@gitnexus ...`
  invocation is explicitly framed as a Claude Code specific host example
- preserve Codex as part of the repository's primary maintained CLI surface
  without claiming equivalent prompt syntax where that host-specific behavior
  is not separately documented here
