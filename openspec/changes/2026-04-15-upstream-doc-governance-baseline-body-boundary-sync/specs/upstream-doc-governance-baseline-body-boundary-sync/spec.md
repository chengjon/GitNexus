# upstream-doc-governance-baseline-body-boundary-sync Specification Delta

## ADDED Requirements

### Requirement: GitNexus SHALL keep the preserved upstream doc governance convergence baseline body clearly bounded from current live replay reading

GitNexus SHALL keep the preserved body sections in
`docs/audits/2026-04-06-upstream-doc-governance-convergence-baseline.md`
explicitly marked as first-pass 2026-04-06 refreshed-fetch baseline context.

#### Scenario: A maintainer reads below the top status-note pointers

- **WHEN** they continue into the `Refresh Summary`, hotspot inventory, or
  `Recommended Replay Order` sections
- **THEN** the page explicitly warns that those counts, inventories, and
  sequencing notes remain the first 2026-04-06 refreshed-fetch baseline
- **AND** the page explains that the later shared replay review and follow-up
  status-sync records control current live replay reading
